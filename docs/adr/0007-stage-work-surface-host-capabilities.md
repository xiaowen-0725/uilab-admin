# Stage Work Surface Host capabilities

Work Surface Host 是 Workbench Shell 的核心 Module，但首版只提供一个可显示、隐藏、调宽和最大化的工作区域，并通过 Tabs 切换已注册的 Surface。Interface 保留未来演进为 Multi-pane 的可能，但首版不提供任意拆分、拖拽重排或多 Pane 同时编排，以先验证 Task Surface 与 Markdown、Browser、Review、Terminal 等工作面的核心协作闭环。
