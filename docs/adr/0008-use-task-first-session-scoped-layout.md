# Use task-first session-scoped layout

新任务默认只显示 Task Surface；当用户或 Agent 打开文件、Browser、Review 等工作内容时，Workbench Shell 才展开 Work Surface Host。Work Surface 的显隐、宽度、活动 Tab 与最大化状态按任务保存并在重新进入时恢复，不使用跨任务共享的全局布局状态，以保留任务上下文连续性并避免不同任务互相污染工作空间。
