# Model provider — copy to .env (gitignored)
VOLTAGENT_MODEL_PROVIDER=deepseek
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
# Preferred: deepseek-v4-flash (default) | deepseek-v4-pro
# Legacy (being discontinued): deepseek-chat | deepseek-reasoner
VOLTAGENT_MODEL=deepseek-v4-flash
# DeepSeek uses Chat Completions so reasoning_content survives multi-step tools.
# VOLTAGENT_MODEL_API=chat
# OpenAI alternative: set provider=openai + OPENAI_API_KEY; responses is then optional.
# VOLTAGENT_MODEL_PROVIDER=openai
# OPENAI_API_KEY=
# OPENAI_BASE_URL=https://api.openai.com/v1
# VOLTAGENT_MODEL=gpt-5
# VOLTAGENT_MODEL_API=responses

# Agent profile: minimal (DIY tools, default) | office (Agent + Workspace FS)
# AGENT_PROFILE=office

# Optional
# PORT=3141
# WORKSPACE_ROOT=/absolute/path/to/your/office-folder
# Office without WORKSPACE_ROOT → ~/VoltAgent-Office/workspace (O2 safe default)
# Never defaults to $HOME itself or the monorepo root for office.
# First boot creates the directory + README.md (does not overwrite).
# VOLTAGENT_MAX_STEPS=50
# LOG_LEVEL=info

# O5 long-run defaults (office)
# VOLTAGENT_MAX_STEPS=80
# VOLTAGENT_SUMMARIZATION=true
# VOLTAGENT_MEMORY=libsql
# VOLTAGENT_MEMORY_URL=file:/absolute/path/to/memory.db

# --- Plugin system (PluginRegistry) ---
# Assembly loads only via createPluginRegistryFromEnv (no hard-coded dual-connector loop).
# Builtins: mcp.docs, mcp.calendar, skills.office (default on), cli.feishu (opt-in).
# 产品级连接器：GitHub 默认内置并走平台 UI Lab Connector 一键授权；飞书走官方 CLI
# PLUGINS_ENABLED=cli.feishu
# 平台部署项（发行/运维配置，不要求终端用户设置）：
# UILAB_CONNECTOR_BROKER_URL=https://connectors.example.com
# MCP_GITHUB_URL=https://api.githubcopilot.com/mcp/
# PLUGINS_DISABLED=mcp.calendar
# PLUGIN_PATHS=/abs/path/to/plugins,/abs/path/to/one/plugin.json
# FEISHU_CLI_PATH=/usr/local/bin/lark-cli
# FEISHU_SKILLS_ROOT=/absolute/path/to/.agents/skills
# Official package: npm i -g @larksuite/cli@1.0.85  (bin: lark-cli)
# Office generic Shell: every execute_command requires Host approval.
# WORKSPACE_SANDBOX_ISOLATION=sandbox-exec  # auto-detected by default
# WORKSPACE_SANDBOX_ALLOW_NETWORK=1
# Capability Surface golden path (real model, not Fake):
#   AGENT_PROFILE=office PLUGINS_ENABLED=cli.feishu pnpm dev
#   pnpm capability:golden

# Optional MCP via builtins mcp.docs / mcp.calendar. Omit = disabled (local FS + Skills only).
# MCP_DOCS_URL=https://your-docs-mcp.example
# MCP_DOCS_BEARER_TOKEN=
# MCP_CALENDAR_URL=https://your-calendar-mcp.example
# MCP_CALENDAR_BEARER_TOKEN=
# Or stdio:
# MCP_DOCS_COMMAND=npx
# MCP_DOCS_ARGS=-y,@example/feishu-docs-mcp
# MCP_CALENDAR_COMMAND=npx
# MCP_CALENDAR_ARGS=-y,@example/calendar-mcp
# MCP_TIMEOUT_MS=20000
# MCP_CHILD_ENV_KEYS=FEISHU_APP_ID,FEISHU_APP_SECRET
# Stdio child env: connector-scoped (docs ≠ calendar secrets)
# MCP_DOCS_CHILD_ENV_KEYS=CUSTOM_DOCS_TOKEN
# MCP_CALENDAR_CHILD_ENV_KEYS=CUSTOM_CAL_TOKEN
# MCP_CHILD_ENV_KEYS=  # shared non-secrets only; model API keys never forwarded
# MCP_READ_ONLY_TOOL_NAMES=docs_read_document,list_calendars
# Auth: enable ≠ login — doctor shows auth=missing until env/cli_session is satisfied.
# Secrets only in this gitignored .env / process env / future keychain — never in plugin.json.
