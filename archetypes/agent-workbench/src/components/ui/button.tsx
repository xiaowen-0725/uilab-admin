/**
 * Compatibility re-export — implementation lives in @uilab/foundation.
 * Application code imports from @/components/ui/button (shadcn Base UI path).
 */
export { Button, buttonVariants } from '@uilab/foundation/ui/button'
