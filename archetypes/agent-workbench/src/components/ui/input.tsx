/**
 * Compatibility re-export — implementation lives in @uilab/foundation.
 * Application code imports from @/components/ui/input (shadcn Base UI path).
 */
export { Input } from '@uilab/foundation/ui/input'
