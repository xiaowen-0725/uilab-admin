---
description: Workbench 日常运行必须同时开前端和侧车
alwaysApply: true
---

# Workbench 双进程

说「跑 Workbench / 验收 / 看对话或看板」时，**必须同时开两个进程**，不要只开前端：

```bash
pnpm dev:workbench              # 前端 http://localhost:5174/
pnpm dev:workbench-runtime      # 侧车 http://localhost:3141
```

只开前端：页面能看，对话走不了 Runtime，有作业的看板刷新会报运行时未连接。取数作业还需要本机 PATH 上有 Deno。
